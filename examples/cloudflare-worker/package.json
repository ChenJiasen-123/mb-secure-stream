{
  "name": "mb-secure-stream-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker using MoonBit-compiled JWT security gateway",
  "main": "src/worker.ts",
"scripts": {
  "build": "cd ../../ && moon build --target wasm-gc --release",
  "dev": "wrangler dev",
  "deploy": "wrangler deploy",
  "tail": "wrangler tail"
},
  "keywords": [
    "cloudflare-workers",
    "jwt",
    "security",
    "moonbit",
    "wasm",
    "typescript"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.1",
    "typescript": "^5.4.5",
    "wrangler": "^4.103.0"
  }
}
